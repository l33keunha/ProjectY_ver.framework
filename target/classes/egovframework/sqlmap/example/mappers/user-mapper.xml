<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ybs.user.common.service.impl.UserMapper">
		
	<select id="checkUser" parameterType="uVO" resultType="egovMap">
		SELECT 
			CASE WHEN ID = #{id} AND PWD = #{pwd} THEN 'exist' 
			      WHEN ID = #{id} THEN 'idOK'
			 END STATE
		FROM USER_INFO
		WHERE 1=1
			AND (ID = #{id} AND PWD = #{pwd})
			OR (ID = #{id})
	</select>
	
	<select id="login" parameterType="uVO" resultType="uVO">
		SELECT 
			*
		FROM USER_INFO
		WHERE 1=1
			AND ID = #{id}
			AND PWD = #{pwd}
	</select>
	
</mapper>