<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ybs.indicator.timeDistn.service.impl.TimeDistnMapper">

	<!-- 1일 평균 통행시간 -->
	<select id ="selectTimeDistnResultListDayTime" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 1일 평균 통행시간 일별 -->
	<select id ="selectTimeDistnResultListDayTime_d" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 목적통행 평균 통행시간 -->
	<select id ="selectTimeDistnResultListPurposeTime" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 목적통행 평균 통행시간 일별 -->
	<select id ="selectTimeDistnResultListPurposeTime_d" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 수단통행 평균 통행시간 -->
	<select id ="selectTimeDistnResultListMethodTime" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 수단통행 평균 통행시간 일별 -->
	<select id ="selectTimeDistnResultListMethodTime_d" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 노선별 평균 통행시간 -->
	<select id ="selectTimeDistnResultListRouteTime" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 노선별 평균 통행시간 일별 -->
	<select id ="selectTimeDistnResultListRouteTime_d" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 1일 평균 통행거리 -->
	<select id ="selectTimeDistnResultListDayDistn" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 1일 평균 통행거리 일별 -->
	<select id ="selectTimeDistnResultListDayDistn_d" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 목적통행 평균 통행거리 -->
	<select id ="selectTimeDistnResultListPurposeDistn" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 목적통행 평균 통행거리 일별 -->
	<select id ="selectTimeDistnResultListPurposeDistn_d" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 수단통행 평균 통행거리 -->
	<select id ="selectTimeDistnResultListMethodDistn" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 수단통행 평균 통행거리 일별 -->
	<select id ="selectTimeDistnResultListMethodDistn_d" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 노선별 평균 통행거리 -->
	<select id ="selectTimeDistnResultListRouteDistn" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
	<!-- 노선별 평균 통행거리 일별 -->
	<select id ="selectTimeDistnResultListRouteDistn_d" parameterType="sVO" resultType="egovMap" fetchSize="1000">
	</select>
	
</mapper>
